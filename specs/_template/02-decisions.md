# 决策记录：{{功能名称}}

> 只记录已经影响方案的结论，不复制讨论过程。按“结论来源”分区：用户或团队直接给出的结论放在人工决策；AI/`/autoplan` 从代码和约束推导出的结论放在 AI 自动决策。AI 结论即使经用户确认，仍保留在 AI 区并标注确认状态。

## 1. 人工决策

### 1.1 范围与优先级

- `YYYY-MM-DD` `[用户确认]`：{{决策内容}}（{{原因/背景}}）

### 1.2 业务语义与交互

- `YYYY-MM-DD` `[用户确认]`：{{决策内容}}（{{原因/背景}}）

### 1.3 权限与安全

- `YYYY-MM-DD` `[用户确认]`：{{决策内容}}（{{原因/背景}}）

### 1.4 工程约束与交付方式

- `YYYY-MM-DD` `[用户确认]`：{{决策内容}}（{{原因/背景}}）

### 1.5 已替代的人工决策

- `YYYY-MM-DD` `[已由 YYYY-MM-DD 替代]`：{{旧决策}} → {{新决策}}

## 2. AI 自动决策与 `/autoplan` 结论

状态说明：

- `[自动采纳]`：由代码事实和已确认约束直接推导，不新增产品范围。
- `[用户已确认]`：最初由 AI 提出，用户已明确确认。
- `[待确认]`：会改变产品范围或关键取舍，尚未得到人工确认。
- `[已替代]`：历史自动结论，已由后续决策替代。
- `[拒绝/延期]`：审查时考虑过，但不进入当前 change。

### 2.1 `/autoplan` 总结

| Phase | 状态 | 最终结论 |
| --- | --- | --- |
| CEO | {{CLEAN/WARNING}} | {{范围和策略结论}} |
| Design | {{CLEAN/WARNING}} | {{交互与视觉结论}} |
| Engineering | {{CLEAN/WARNING}} | {{架构、正确性和测试结论}} |
| DevEx | {{CLEAN/WARNING}} | {{开发体验和生成流程结论}} |
| 简化审查 | {{PASS/WARNING}} | {{明确未引入的抽象或平台}} |

### 2.2 架构与技术选型

- `YYYY-MM-DD` `[自动采纳/用户已确认]`：{{结论}}（证据：`path/to/file:line`；原因：{{说明}}）

### 2.3 数据模型、API 与迁移

- `YYYY-MM-DD` `[自动采纳/用户已确认]`：{{结论}}（{{说明}}）

### 2.4 边界、失败处理与测试

- `YYYY-MM-DD` `[自动采纳/用户已确认]`：{{结论}}（{{说明}}）

### 2.5 设计与 DevEx

- `YYYY-MM-DD` `[自动采纳/用户已确认]`：{{结论}}（{{说明}}）

### 2.6 已替代、拒绝与延期

- `YYYY-MM-DD` `[已替代/拒绝/延期]`：{{结论}}（{{原因以及重新评估条件}}）

## 3. 未决事项

> 这里只列真正需要人工选择的事项；没有时明确写“无”。

- 无。
