# 设计决策记录

> 每次 spec 讨论中达成共识的决策即时记录在此。每行格式：`YYYY-MM-DD: 决策内容（原因/背景）`

## 范围 & 优先级

- `YYYY-MM-DD`: [内容] — 例如：P1 功能降为 P2（开发成本高，价值不确定）

## 架构 & 技术选型

- `YYYY-MM-DD`: [内容] — 例如：使用 PostgreSQL JSONB 存储变更详情（无需单独建表，查询灵活）

## 边界 & 异常处理

- `YYYY-MM-DD`: [内容] — 例如：审计写入失败时静默兜底，不阻塞主流程

## 数据模型

- `YYYY-MM-DD`: [内容] — 例如：asset_id 允许为 0（存在不关联具体资产的操作）

## 其他

- `YYYY-MM-DD`: [内容]
